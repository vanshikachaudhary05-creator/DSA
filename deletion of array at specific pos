#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
int n,pos,num;

cin>>n>>pos;
vector<int >arr(n);
for(int i=0;i<n;i++){
    cin>>arr[i];
}
  if(pos<=0||pos>n){
      cout<<"invalid pos";
  }
      else{
          for(int i=pos-1;i<n-1;i++){
              arr[i]=arr[i+1];
          }
            n--;
      }
  
    

    
for(int i=0;i<n;i++){
    cout<<arr[i]<<" ";
}
return 0;
}
